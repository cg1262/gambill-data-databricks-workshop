bundle:
  name: salesforce_lakeflow_bundle

include:
  - resources/*.yml 

variables:
  env:
    default: dev 
  
targets:
  dev:
    variables:
      env: dev
    mode: development
    default: true
    workspace:
      host: https://dbc-d641e3ea-b894.cloud.databricks.com

  test:
    variables:
      env: test
    mode: production
    workspace:
      host: https://dbc-d641e3ea-b894.cloud.databricks.com
      root_path: /Users/chris.gambill@gambilldataengineering.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      user_name: chris.gambill@gambilldataengineering.com

  prod:
    variables:
      env: prod
    mode: production 
    workspace:
      host: https://dbc-d641e3ea-b894.cloud.databricks.com
      root_path: /Users/chris.gambill@gambilldataengineering.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      user_name: chris.gambill@gambilldataengineering.com


resources:
  schemas:
    sales:
      name: sales
      catalog_name: silver_${var.env}
    sales_ops:
      name: sales_ops
      catalog_name: silver_${var.env}